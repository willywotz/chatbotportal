# Self-hosted OIDC. VITE_OIDC_AUTHORITY must equal the backend OIDC_ISSUER and
# the SPA's public root origin. Empty authority falls back to
# `window.location.origin` at runtime (see src/shared/lib/oidc.ts).
VITE_OIDC_AUTHORITY=http://localhost:8080
VITE_OIDC_CLIENT_ID=portal-spa
