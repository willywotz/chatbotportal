# MEMORY — AI Chatbot Portal (Thai Citizen Guide)

Living source of truth. Prune on every change. Distilled from the retired `CONTEXT.md`; dated changelog dropped (it lives in Git history).

## Current Focus
- ✅ LLM pluggable LangChain layer DONE (TDD, 17 tasks, native/inline) on branch `feat/llm-pluggable-langchain`; backend **778 pass**, only known pre-existing `test_router_smoke` fails (`test_orm_models` now FIXED here). Old httpx `client.py` + `llm_providers`/`llm_routes` REPLACED. Deps `langchain`/`langchain-core`(**1.6.x**)/`langchain-openai`(1.6.x)/`openai`(2.x) — resolver picked 1.x over the plan's 0.3.x floor; all needed APIs present (`init_chat_model`, `get_usage_metadata_callback`, `BaseRateLimiter`, `GenericFakeChatModel`). Service layout `app/features/llm/services/`: `service.py`(chat/parse/ping under `get_usage_metadata_callback`, errors→`to_llm_error`, usage recorded in `finally`) · `resolve.py`(purpose→`ResolvedRoute`, 30s cache; no fallback) · `model_factory.py`(build_model→bind_tools/with_structured_output→`.with_retry`, passes `RedisRateLimiter`) · `providers.py`(registry kind→`ProviderSpec`; `openai` registered; unknown kind→`LlmError(kind="config")`; ⚠️ `_SPECS` is a GLOBAL — test fake kinds need unique names) · `rate_limit.py`(added `RedisRateLimiter`/`_allow`/`limiter_for`/`reset_cache` over the kept `PostgresFixedWindowLimiter`) · `purpose.py`+`result_schemas.py`(`Purpose`, `PURPOSE_SCHEMAS`; Classification/Judge/Spec/PopularQuestions results, PopularQuestionItem has `score`) · `errors.py`(`LlmError(msg,*,kind,status=None,provider=None)`+`to_llm_error`) · `usage.py`(`record(session, usage_metadata_dict, *, purpose,…)` from the callback aggregate). Tables `llm_provider`/`llm_binding`/`llm_usage` via Alembic `0006` (drops old two; downgrade recreates). Repos `repositories/llm.py` = providers+bindings (`enabled_binding_for_purpose`/`provider_has_bindings`/`binding_purpose_exists`/`get_provider_by_name`). Admin `/language-model`: `GET /kinds`+`/purposes`, providers CRUD, bindings CRUD (unknown kind→422), `POST /bindings/{purpose}/test`→ping; `api_key` masked (`MASK`), mutations audit+`invalidate()`. Env `LLM_DEFAULT_MODEL`/`LLM_BASE_URL`/`LLM_API_KEY`(secret). Seed = one `default` openai provider + a binding per purpose (idempotent). Call sites → typed `parse()`/`chat()`: classify→`ClassificationResult.category`, judge→`JudgeResult`, parse_spec→`SpecResult.model_dump()`, popular→`PopularQuestionsResult`, brief→`chat().content`. Tests never hit network (fake `ProviderSpec.build_model=GenericFakeChatModel`); resolve/rate-limit caches reset via autouse fixture in `tests/conftest.py`. **Web updated too**: `features/llm-providers/` (add kind from `GET /kinds` + model, drop auth_header/auth_scheme/request_usage, base_url optional, expose max_retries) and `features/llm-routes/`→**`features/llm-bindings/`** (edit `/bindings`: model_override, timeout_override; test `/bindings/{purpose}/test`); `LLMBindingResponse` now returns `model_override` so the edit UI round-trips it; web LLM tests + tsc + lint clean (12 pre-existing localStorage/text-scale fails remain). **Next actionable: open PR to `main` (superpowers:finishing-a-development-branch).**
- Agency admin wizard REPLACED by a single-page form (TDD) — MERGED to `main` (`52c7346`); web 378 pass (only the known 11 text-scale/localStorage pre-existing fails), typecheck + lint clean. Deleted `web/src/features/agencies/wizard/` (4-step `AgencyWizardPage` + `Step{General,Connection,Routing,Review}` + stepper nav + `StepReview` + wizard tests). New `web/src/features/agencies/form/`: `AgencyFormPage` (all field groups on ONE page as `Card`s — desktop `md:grid-cols-2`: General+Routing left column, Connection tall right via `md:row-span-2`; actions = บันทึก Draft / เปิดใช้งาน; resume `/agencies/:id/setup` hydrates the whole form, no step jump) + `GeneralFields`/`ConnectionFields`/`RoutingFields` (renamed from the Step components). `HeadersEditor` moved to `features/agencies/HeadersEditor.tsx` (shared with `detail/ConnectionTab`). Routes `/agencies/new` + `/agencies/:id/setup` unchanged → `AgencyCard`/`AgencyDetailPage` "ตั้งค่าต่อ" links + `roles.ts` untouched. `agencyForm.ts`: dropped wizard-only `WIZARD_STEPS`/`WizardStepId`/`firstIncompleteStep`; renamed `isStepGeneralValid`→`isGeneralValid`, `isStepConnectionValid`→`isConnectionValid`. Agency DETAIL edit is now the same `AgencyFormPage` via new route `/agencies/:id/edit` (guarded ADMIN in `roles.ts`): deleted the detail "แก้ไข" tab + `detail/{EditTab,GeneralSection,ConnectionTab,RoutingTab}.tsx` + `editTabs.test.tsx`; detail header now has an "แก้ไข" `Link`-button → `/agencies/:id/edit`; `AgencyCard` kebab "แก้ไข" now navigates `/agencies/:id/edit` (was `?tab=edit`); dropped the detail `?tab=edit` deep-link. ⚠️ Trade-off: the old `ConnectionTab` ADR-0002 confirm dialog (warn before demoting active→draft on identity edit) is GONE — backend `_demote_or_validate` still enforces the demotion, so it is a lost warning, not a lost guard.
- Agency conformance FULLY REMOVED (TDD) on branch `refactor/remove-agency-conformance` off `main`; backend 754 pass + web 399 pass (only the known pre-existing fails: 2 backend, 11 web text-scale). Next actionable: open the PR to `main` (superpowers:finishing-a-development-branch). Deleted: `services/conformance.py` (+ `run_conformance`, the 5-check battery), the `POST /agencies/{id}/conformance` route, the `agencies.conformance_report` column (Alembic `0005` drops it; downgrade re-adds nullable JSONB), and the web wizard "test" step (`StepTest`, `useRunConformance`, `ConformanceReport` type, wizard `test` step id). `_ask` (dispatch-one helper) relocated into `services/evaluation.py`, its only remaining user. `draft → active` is now a plain legal transition (no report gate): create/replace/status-update into `active` is allowed; wizard review + agency-detail dropdown activate a draft directly.
- Monitoring follow-ups 1/2/3/5 + Health-UI DONE (TDD) on branch `fix/monitoring-followups` off merged `main`; backend suite 747 pass + web 401 pass, only the known pre-existing fails (2 backend, 11 web text-scale). Next actionable: open the PR to `main` (superpowers:finishing-a-development-branch). Changes: (1) `cs_repo.get_for_update` (`SELECT … FOR UPDATE`) now backs BOTH record paths — `agency.run_connection_test` + `monitor._record_one` — so a manual test and a concurrent tick serialize on the `agency_check_state` row (no lost `consecutive_failures`). (2) deleted dead `agency_reconcile.py` + its 8 tests + the orphaned `agency_repo.list_by_statuses`. (3) `analytics/services/health.py` now sources uptime from `bucket_repo.uptime_by_agency`, not `error_window`/ConnectionLog; latency still from ConnectionLog query rows. (5) `leased_until` crash-reclaim implemented — Alembic `0004` adds nullable `agency_check_state.leased_until`; `claim_due(…, lease_seconds=)` stamps `now+MONITOR_LEASE_SECONDS`(=120) on claim and reclaims rows whose lease expired via `OR leased_until <= now`; `record_result` clears it. `run_tick` passes the lease.
- Health-UI (extends 3): `/agency-health` now returns real data — `Agency` schema gained `uptime7d`/`uptime30d` (buckets: 24h/7d hour grain, 30d day grain), and `incidents` populated from `incident_repo.recent(limit=20)` (open first) mapped to the `Incident` schema (`type=downtime`, `severity=critical` while open else `warning`, `message=detail`, `occurredAt=started_at`, `resolvedAt=ended_at`). `slaCompliance` still `[]`. Frontend `healthApi.AgencyHealthData` mirrors it (`resolvedAt` now nullable); `HealthPage.tsx` renders 24h/7d/30d uptime rows + an Incidents card; MSW `MOCK_AGENCY_HEALTH` seeds the new fields.
- Rebuild recap (merged `8c00d57`): `features/monitoring/` slice — models `agency_check_state`/`uptime_bucket`(hour+day dual-write)/`incidents`; claim-based sharded `monitor_tick` (`FOR UPDATE SKIP LOCKED` on `agency_check_state` + `next_check_at` bump = exactly-once across 4 workers); `retry_async`+jitter; self-heal `ensure_states`. `_record_one` probes with NO DB session held, records in a separate short txn. Sync state/bucket/incident writes + `agency.incident_opened|closed` outbox → `event_consumers._on_incident_*` audit projection. `public_status` sums buckets (24h/7d hour, 30d day; empty→`null`) + `incident_open`. `event_repo.pending` is `FOR UPDATE SKIP LOCKED`. Cutover retired `agency_chat_test` + `reconcile_statuses` job, stopped `ConnectionLog action=test` writes, removed `AGENCY_CHAT_CONCURRENCY`.
- Remaining deferred: (4) `AGENCY_CHAT_TIMEOUT` kept (chat dispatch + agent-proxy). (6) no uptime-history backfill; external alert delivery deferred. `agency_health.py::error_window`/`embedded_health`/`health_history` (the `/agencies` embed) still read ConnectionLog and share the same test-row staleness — not migrated (out of follow-up-3 scope). (7) ⚠️ **SSRF accepted as a known risk (operator decision):** `probe_reachability` fetches admin-configured `agency.endpoint_url` with no scheme/private-range validation and no DNS-rebind protection → a staffer with `agency:manage` can reach internal/metadata hosts. Follow-up: gate probe targets if all endpoints are public.
- ✅ Agency lifecycle invariant centralized (`ef61af2`, TDD): `agency_lifecycle.assert_legal_transition(current, target)` is the ONE gate — no-op when `current == target`, else `target` must be in `LEGAL_TRANSITIONS` (no conformance report arg anymore). `transition_status` delegates to it. `services/agency.py` routes EVERY write path through it: `create_agency` validates as a transition from an implicit `draft`, so `active`/`disabled` on create ARE allowed (draft→active is legal) but `maintenance` is refused (422) — seed still creates `active` via `agency_repo.create` directly = the trusted bootstrap path. `replace_agency` (PUT) + `update_agency` (PATCH) share `_demote_or_validate`: a connection-identity change on an `active`/`maintenance` agency forces `draft` (ADR 0002, enforced on PUT too), otherwise the status change is validated. Identity compare normalizes falsy→`None` (`x or None`) so PUT's full-payload `[]` no longer spuriously demotes vs a stored `None`. `AgencyBase.status` default stays `active` (used only by responses); a raw create/replace that omits status is refused, so clients must send `draft`.
- ⚠️ Residual audit finding (NOT yet fixed, out of scope): `crud.increment_calls` returns `AgencyResponse.model_validate` with NO `_with_health` embed (health=null) unlike every other CRUD handler.
- ⚠️ Finding (pre-existing): scheduler starts in the FastAPI lifespan (`main.py:91`) under `uvicorn --workers 4`. The outbox (`event_repo.pending`) and the new `monitor_tick` are now worker-safe (row locks). The other duplicated jobs (brief/popular/eval/purge) still run 4× — flagged follow-up.
- Infra drift vs older notes below (verified in `compose.yaml` this session): compose now uses **nginx** (not caddy) and **`postgres:16`** (downgraded from 18). Treat the caddy/postgres:18 mentions in Services/Pitfalls as stale.
- Pre-existing test failures (NOT this work): backend `test_router_smoke::test_health_returns_200` (hits `/health`; route is `/healthz`); frontend 11 text-scale tests (`window.localStorage` undefined in env). (`test_orm_models::test_all_15_tables_registered` was fixed on the LLM branch: it now imports `app.core.registry` and asserts the reshaped `llm_provider`/`llm_binding` tables.)
- Pre-existing stale script (NOT from this work): `backend/scripts/hash_existing_api_keys.py` imports `app.auth.security` (removed pre-OIDC); dead, not imported by app/tests.

## Active Status
- [x] SQLAlchemy 2 async + Alembic baseline (`versions/0001_initial.py`); no PGroonga (removed). App is Tortoise-free (0 refs in `app`/`tests`).
- [x] Repository layer (`app/repositories/*`, `session`-first module funcs). `get_db` owns one txn/request.
- [x] Self-hosted OIDC provider/IdP (replaced Keycloak). Local `users` table; RS256 signing key in DB; role→scope map in `app/auth/scopes.py`.
- [ ] Stale Aerich doc references (readme + docs).
- [ ] `testcontainers.postgres` deprecation → `testcontainers.community.postgres` (`tests/conftest.py:26`).

## What this is
AI gateway/one-stop portal: routes Thai citizens' NL questions to Thai gov agencies (API/MCP/A2A), synthesizes one cited answer. Heavy orchestration (decompose→route→dispatch→synthesize, sync v1–v3, stream v4/v5) runs in external **OneChat** (`ONECHAT_BASE_URL`, via `services/onechat/`). This backend is the portal/gateway: wraps OneChat, serves an MCP server OneChat calls back into, persists conversations, and owns all admin/analytics/auth. Product name "AI Chatbot Portal"; repo/README still "Thai Citizen Guide".

## Deploy targets
- **Prod = compose.yaml via Ansible** (`deploy/deploy.yml`, GHCR images `ghcr.io/willywotz/chatbotportal{,-web}:<tag>`, Cloudflare fronts TLS → nginx gateway serves plain HTTP on host 80).
- **`deploy/k3s/`** = single-VPS k3s alternative to compose, one file per service (`namespace`/`config`/`postgres`/`jaeger`/`backend`/`web`/`gateway`.yaml + `kustomization.yaml` + `README.md`); apply with `kubectl apply -k deploy/k3s` (kustomize orders ns+config first). ns `chatbotportal`; 5 Deployments (postgres/backend/web/jaeger/gateway) + Services; postgres on `local-path` PVC 10Gi (`Recreate`). Edge chain **Cloudflare → Traefik (k3s built-in) → Ingress `gateway` (`ingressClassName: traefik`, catch-all `/`) → gateway nginx (ClusterIP) → backend/web/jaeger**. Gateway = stock `nginx` + `gateway-nginx` ConfigMap holding the SAME routing as `deploy/nginx/nginx.conf` (docker `resolver`/`set $upstream` dropped, direct `service:port` upstreams); nginx keeps the fine-grained path routing, Traefik just fronts it host-wide. Cloudflare-fronted plain HTTP (no in-cluster TLS). Placeholders: replace `REPLACE_WITH_*` secrets (`config.yaml`) + image tags (`backend`/`web`.yaml) + Ingress host (`gateway.yaml`) before apply.

## Services (`compose.yaml`)
All traffic via **caddy** (80/443, TLS auto via `CERT_DOMAIN`; `caddy/Caddyfile` = single routing+TLS source). `backend` Python 3.12 · FastAPI · SQLAlchemy 2 async · Alembic · FastMCP (`/api/v1`, `/mcp`, port 8080, `uvicorn --workers 4` → MCP stateless-http). `web` React 18 · Vite 5 · TS · shadcn/ui. `postgres` = `postgres:18` (plain; no PGroonga). `jaeger` = `jaegertracing/jaeger:2.18.0` (OTLP `jaeger:4317`, UI `/jaeger/`). `agent-proxy` (Go) reverse-proxies agency calls, logs every call, bumps `total_calls` on 2xx.
- Caddy `/api /sse /messages /mcp /docs /redoc /openapi.json`→backend (`/api/v1/responses` gets its own 3700s WS timeout block); `/.well-known/openid-configuration`, `/.well-known/jwks.json`, `/oauth2/*`→backend (OIDC provider; only those two well-known paths, so `/.well-known/acme-challenge/*` stays with Caddy); `/jaeger/*`→jaeger; `/`→web SPA (incl. `/auth/callback`).

## Request flow (chat)
`POST /api/v1/chat` (JSON, or SSE when body `stream:true`); `WS /api/v1/chat` same path. `POST /api/v1/responses` = OpenAI Responses-compatible (HTTP/SSE/WS; model id `onechat` only, version via `onechat_version` body field). All transports drive one transport-free pipeline `prepare_turn`+`run_turn` (`services/chat/stream.py`). No answer cache: every turn streams live (similarity cache removed).
1. **OneChat dispatch** via `services/onechat/` (`get_client(version)`, `resolve_version()` → newest v5). Client is transport-only (errors: non-200→status, `ReadTimeout`→504, other→502=`OneChatError`); persistence/tracing stay in callers. All paths from `ONECHAT_BASE_URL`. v5 adds `summarize` step, `summary`, `references[]`, `thread_name`; degrades silently to v4. Existing convos first `ensure_session_warmed()`.
2. **Persist**: `services/chat/turn.save_turn()` → Conversation + user/assistant Message + `ConnectionLog(action=query)`. `thread_name` titles the convo only on the creating turn.
3. **Classify** (background): `features/chat/services/llm.classify_message_category()` tags a Thai category via `llm.parse(Purpose.CLASSIFICATION)` → `ClassificationResult.category`.

## Data model (SQLAlchemy 2 declarative, `app/models/`)
- Access via **module-level repo funcs** (`session` first arg). Routers `Depends(get_db)` = one txn/request, commit-on-success. Non-request contexts (scheduler, MCP, seed, chat-stream persist, agent-proxy, rate-limit, outbox dispatch) open own short `AsyncSessionLocal()`+`begin()`. **Services never `commit()`**; use `flush()` for mid-txn PKs.
- Enum cols = `Enum(EnumClass, native_enum=False, create_constraint=False, length=n)` (varchar, coerced on read; app uses `.value`). All datetimes `timestamptz`. `Base` sets `eager_defaults=True` + naming convention (clean Alembic diffs).
- 22 tables: `agencies`, `conversations`, `messages`, `connection_logs`, `golden_questions`, `eval_results`, `executive_briefs`, `llm_provider`, `llm_binding`, `llm_usage`, `audit_logs`, `settings` (PK=`key`, no id), `popular_questions`, `domain_events` (outbox), `rate_limit_counters` (BigInt PK + unique(key,window_start)); OIDC: `users`, `signing_keys` (PK=`kid`), `oauth_auth_codes` (PK=`code_hash`), `oauth_refresh_tokens`; monitoring (Alembic `0003`): `agency_check_state` (PK=`agency_id`, FK→`incidents` `use_alter`, nullable `leased_until` added by `0004`), `uptime_bucket` (unique `(agency_id,granularity,bucket_start)`), `incidents` (partial-unique `(agency_id) WHERE ended_at IS NULL`). Alembic `0002` adds the four OIDC tables; `0003` the three monitoring tables; `0004` the `leased_until` crash-reclaim column; `0005` drops `agencies.conformance_report`; `0006` reshapes LLM: drops `llm_providers`/`llm_routes`, creates `llm_provider` (adds `provider` kind col, drops `auth_header`/`auth_scheme`/`request_usage`, `base_url` nullable) + `llm_binding` (`purpose`, `provider_id`→`llm_provider` RESTRICT, `model_override`, `timeout_override` (no fallback)).
- `Conversation.meta` = Python attr mapped to DB col `metadata` (SQLAlchemy reserves `Base.metadata`). `user_id` = plain nullable UUID (OIDC sub → `users.id`; no hard FK). `Message.summary_references` NOT `references` (reserved word). `LlmUsage.total_tokens` = Python `@property`.
- **Migrations = Alembic only** (`backend/alembic/`, async env sourcing URL from settings). Tortoise/Aerich squashed into `0001_initial` (15 tables w/ FK ondelete; no PGroonga extension/index). `init_db()` runs `alembic upgrade head` at startup (serialized across workers by a Postgres advisory lock; NO `create_all`/`generate_schemas`). Schema change = edit model → `uv run alembic revision --autogenerate -m "..."`, review diff.

## Auth & RBAC (self-hosted OIDC — authoritative, supersedes all Keycloak/cookie/JWT notes)
- Backend IS the OpenID Provider (`app/auth/oidc/`): issuer = root origin (`OIDC_ISSUER`, no path); discovery/jwks at `/.well-known/{openid-configuration,jwks.json}`, authorize/token/userinfo under `/oauth2/*` (NOT `/api/v1`). Auth-Code+PKCE(S256), RS256, refresh-token rotation (reuse revokes the chain). `/oauth2/authorize` serves a server-rendered login page (HTML-escaped inputs). Access token carries `role`+`scope`; the **id token also carries `role`** so the SPA reads it from the OIDC profile (no `/me`).
- **Signing key**: `signing_keys` table, one RSA keypair generated at startup under a PG advisory lock (shared across workers; distinct lock key from migrations); env override `OIDC_PRIVATE_KEY`. Public keys cached in-memory so `verify_token` (`app/auth/oidc/tokens.py`, RS256, iss=`OIDC_ISSUER`, aud=`OIDC_ISSUER` — no separate audience knob) does no per-request I/O → frozen `Principal{id(sub),email,display_name,role,scopes}` (`app/auth/principal.py`).
- **Per-route scopes, no runtime gate**: each protected route `Security(require_scope, scopes=[…])`. Access-token JWT carries flat `role` + space-delimited `scope`. CI `tests/test_route_audit.py` guarantees no route is silently unprotected.
- **Anonymous = `/api/v1/public/*`** (auth-optional): `public_status`, `popular_questions`, guest chat `POST /public/chat`. Whitelisted non-public: `/api/v1/agent-proxy/{id}` (OneChat callback), `GET /api/v1/authentication/me`.
- Roles→scopes live in **code** (`app/auth/scopes.py`, composite `user`⊂`staff`⊂`admin`), replicating the old realm composites verbatim. Ownership is scope-based (`conversation:read:all` vs filter to `principal.id`); no `role=="admin"` literals in services.
- User mgmt = local `users` table via `services/user_admin.py` (`routers/users.py` under `user:manage`; bcrypt via `app/auth/oidc/passwords.py`, SHA-256 pre-hash); hard delete, refuses self-delete. Startup seeds `SEED_ADMIN_EMAIL` (role admin) if none exists. Config: `OIDC_ISSUER/CLIENT_ID/ALLOWED_REDIRECT_URIS/ACCESS_TOKEN_TTL/REFRESH_TOKEN_TTL/CODE_TTL/PRIVATE_KEY`, `SEED_ADMIN_EMAIL/PASSWORD`; SPA reads **runtime** config from `/config.js` (`window.__APP_CONFIG__.{OIDC_AUTHORITY,OIDC_CLIENT_ID,API_BASE_URL}`), callback route `/auth/callback`.
- **Frontend auth = `react-oidc-context`** (over `oidc-client-ts`), NO bespoke provider/`/me`. `main.tsx` wraps `<App>` in `<AuthProvider {...oidcConfig}>` (`shared/lib/oidc.ts`); `features/auth/useAuth.tsx` is a thin adapter mapping the OIDC profile (id-token claims: `sub/email/name/role`) to `{user,isAdmin,isLoading,signIn,signOut}` (missing role⇒`user`; MOCK mode⇒fixed admin; tolerates no-provider in unit tests). `signIn`=`signinRedirect({state:{returnTo}})`; `signOut`=`removeUser()`+home (no RP-initiated logout endpoint). `CallbackPage` waits for react-oidc-context to process then routes to `returnTo`; `onSigninCallback` strips `?code&state`. `AuthTokenSync` (in App) pushes the access token + 401-relogin into `shared/lib/authToken.ts`, which the non-React `apiClient`/`chatApi`/`useAgencies` read for the `Bearer` header. Verified e2e (login→callback authenticates with no reload; role gates admin nav; scoped API works).

## Scheduler jobs (`app/scheduler.py`)
`monitor_tick` (`MONITOR_TICK_SECONDS`=15s: `ensure_states` self-heal → claim due non-draft/disabled agencies `FOR UPDATE SKIP LOCKED` + bump `next_check_at` → probe each under `MONITOR_PROBE_CONCURRENCY` sem with retry+backoff → record state/bucket/incident in one txn per agency; probe runs with no DB session held), `regenerate_brief_job` (24h), `purge_old_connection_logs` (24h, retention 90d, also prunes `uptime_bucket` hour/day rows), `run_evaluation` (weekly golden-question LLM-judge), `regenerate_popular_questions` (24h: LLM-synth คำถามยอดนิยม from successful turns; no-op below `POPULAR_QUESTIONS_MIN_TURNS`=20; replaces only unpinned/unhidden `auto` rows; hidden `text_key`=tombstone). Old `agency_chat_test` + `reconcile_statuses` job retired (see Current Focus follow-ups).
- **Connection test** = one reachability probe/type (HEAD→GET), any HTTP response (incl 4xx/5xx) = reachable=success; only transport failure = error. No protocol handshake. Threshold `FAILURE_THRESHOLD`=3 consecutive fails opens an `incident`; a success closes it and recovers a `maintenance`+`auto_maintenance` agency to `active`.

## Conventions & Pitfalls
- **Backend layout = feature-based clean architecture.** `app/core/` = shared kernel (config, db, base, errors, concurrency, trace_util, utils, usage_context, log_sanitize, events outbox, audit, connection_log, `security/` guard = principal/scopes/dependencies/tokens, `registry.py` = fills `Base.metadata`). `app/features/<slice>/` each own `routers/services/repositories/schemas/models`. Dependency rule: feature → core only, never feature → feature router. `main.py`/`scheduler.py` = composition roots. Translating older notes here: former `app/models/X` → its feature's `models/X` (or `core/models/X` for event/audit/connection_log); `app/services/X`, `app/routers/X`, `app/repositories/X`, `app/schemas/X` → the owning `app/features/<slice>/…`; `app/auth/*` → `core/security/*` (guard) or `features/identity/oidc/*` (provider). Alembic sources `app.core.registry`.
- Reply in ASD-STE100 Simplified Technical English. Self-documenting code, **no comments** except Swagger/OpenAPI (project rule). KEEP-only comment policy = explain WHY (security/external-quirk/PDPA/async hazard/`withinlazy:` simplification).
- 15-Factor App mandatory. Full-English API route names (no short forms). TDD mandatory (red→green→refactor). Branch before multi-task work; **never** git worktree.
- **All OneChat calls via `services/onechat/`** — never inline an upstream URL. Inject `httpx.MockTransport` to test.
- Agencies router: register literal paths (`/mcp/discover`, `/parse-specification`) **before** `/{agency_id}`; an unmatched literal falls to `/{agency_id}` → **422** (UUID validation), not 404.
- Error envelope `{"error":{"code","message","retryable","upstream_status"}}` (`app/errors.py`); frontend unwraps (legacy `detail` fallback).
- `utils.clean_agency_ids` at every read of `Message.agency_ids` — legacy comma-joined `"id1,id2"` is an invalid UUID that crashes asyncpg `id__in` queries.
- **MCP `endpoint_url` scheme** resolves cf-visitor → X-Forwarded-Proto → connection scheme (`_external_scheme`, `app/mcp/server.py`) — behind Cloudflare the chain speaks http; only `cf-visitor` carries the real https. Covered by `test_mcp_endpoint_scheme.py`.
- Server-rendered `/oauth2/authorize` login page inlines the `web/` SPA design tokens (`web/src/index.css` `:root`/`.dark`) + Sarabun; keep the two in sync when the SPA theme changes (no shared stylesheet — the page is a standalone HTML string).
- **OIDC issuer must match**: `OIDC_ISSUER` (token `iss` + SPA authority) must equal the browser root origin (no path); a mismatch fails `verify_token`'s issuer check. `OIDC_ALLOWED_REDIRECT_URIS` must list the SPA's `/auth/callback`.
- **Frontend config is RUNTIME, not build-time**: the prod image bakes no `VITE_*` values. `web/docker-entrypoint.d/40-app-config.sh` writes `/config.js` (`window.__APP_CONFIG__`) from the web container's `OIDC_AUTHORITY`/`OIDC_CLIENT_ID`/`API_BASE_URL` env at startup; `index.html` loads it (classic script) before the app module. One image runs on any port/domain — change env + recreate, no rebuild. nginx serves `config.js` `no-store` (fixed name, mutable body) while hashed `/assets/*` stay `immutable`. The earlier "unknown client_id"/wrong-port bug was a stale build-time `VITE_OIDC_*` bake; this replaces it. Compose passes the SPA env as `OIDC_AUTHORITY`/`OIDC_CLIENT_ID`/`API_BASE_URL` (defaults track `OIDC_ISSUER` and `HTTP_PORT`).
- `docker compose up` won't rebuild an existing image → config edits do nothing until `up -d --build <svc>`.
- Frontend tests can't use `node` env or import `vite.config.ts` (jsdom@20 under Node 24; `setup.ts` touches `window`).
- Agency activation: `draft→active` is a plain legal transition (no gate; conformance battery removed). Editing connection-identity fields on an active/maintenance agency demotes it to `draft` (ADR 0002). Agency `API` must return HTTP 200 for every valid question.
- ⚠️ `spec/agent-20260623.md`, `agent-onechat.md`, `agent-promes.md` contain **real API keys flagged for rotation** — treat as secrets, do not propagate.
- Tracing: one W3C trace id across user→portal→OneChat→/mcp→agent-proxy. OneChat drops `traceparent` header but keeps query strings → context smuggled via `?traceparent=` on `mcp_endpoint_url`/agent-proxy URLs (`app/trace_util.py`), promoted back to a header on receipt.

## Docs & specs map
`docs/agency-integration.md` (agency contract), `docs/quickstart.md` (API consumer), `docs/tls.md`, `docs/tracing-verification.md`, `spec/roadmap.md`, `spec/openai-responses*.md` (Responses wire contract; 9/53 stream events implemented), `spec/v4-streaming.md`, `spec/mcp-server.md`, ADRs `docs/adr/000{2,3}-*`. `docs/aerich-migrations.md` is now historical (Aerich superseded by Alembic).
