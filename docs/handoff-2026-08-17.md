# Handoff — Refactor to follow project rules (2026-08-17)

Written in Simplified Technical English. This is the state at end of session.

## Goal
"Refactor to follow rule": Clean Architecture, full english API route names,
15-Factor App, and event-driven architecture. Update `CONTEXT.md` and commit.

## Git state
- Work merged to `main` as merge commit `9f6b1d1` (`git merge --no-ff`).
- Source branch: `refactor/full-english-route-names` (12 commits).
- **`main` is NOT pushed** — the merge is local only.
- Working tree is clean.

## What is done (per rule)

### Clean Architecture — COMPLETE
- All 24 routers now call a service and build **zero** ORM querysets.
- Verify: from `backend/`, this prints nothing —
  `for f in $(find app/routers -name "*.py" ! -name "__init__.py"); do grep -lE '[A-Z][A-Za-z]*\.(filter|get|create|save|all|exclude|count|delete|get_or_none|bulk_create|update_from_dict)\(' "$f"; done`
- New service modules: `agency_golden`, `connection_log`, `public_status`, `conversation`,
  `message`, `feedback`, `api_key`, `settings`, `seed`, `analytics/usage`, `analytics/heatmap`,
  `llm/admin`, `openai/conversations`. Extended: `agency`, `agency_lifecycle`, `audit`,
  `popular_questions`, `user`.
- Method: 7 file-disjoint batches ran as parallel worktree-isolated builder agents (TDD each);
  the main context consolidated the results and hand-merged the 4 files two batches touched.

### Full english API route names — COMPLETE
- `/auth` → `/authentication` (incl. the allowlist chokepoint `app/auth/dependencies.py`).
- `/auth/anon` → `/auth/anonymous` (handler `create_anonymous_session`).
- `/llm` → `/language-model`.
- Kept: `/api` and `/mcp` — standard technology proper nouns, like the un-renameable `/api/v1`
  prefix and the external OpenAI/MCP wire contracts. The python module path `app.auth` is code,
  not a route, so it is unchanged.

### Event-Driven Architecture — real seam in place
- `app/models/event.py` `DomainEvent` — outbox table. Migration
  `migrations/models/29_20260817000000_domain_events_outbox.py`.
- `app/services/events.py` — `publish` / `subscribe` / `dispatch_pending`.
- `app/services/event_consumers.py` — `register_consumers()` (idempotent); first consumer:
  `agency.status_changed` → audit projection.
- Producer: `agency_lifecycle.transition_status` publishes `agency.status_changed`.
- Wiring: `scheduler.start_scheduler` registers consumers and adds a `dispatch_pending` tick
  (`EVENT_DISPATCH_INTERVAL_SECONDS`, default 10).

### 15-Factor App — III, XI, XII done; VI a documented exception
- III (config in env): `LOG_LEVEL`, `EVENT_DISPATCH_INTERVAL_SECONDS` in `app/config.py`.
- XI (logs to stdout): `logging.basicConfig(stream=sys.stdout, ...)` in `app/main.py`.
- XII (admin as one-off process): `backend/scripts/seed.py`; the HTTP seed router was removed.
- VI (uploads): **deliberate exception** — agency-logo uploads stay on the local-disk Docker
  volume (`UPLOAD_DIR`) for the single-node deploy, by user decision. See `CONTEXT.md`.

## Verification
- Full backend suite on `main`: **839 pass / 6 skip** (`cd backend && .venv/bin/pytest -q`).
- Frontend auth + language-model tests: **pass**
  (`cd frontend && npx vitest run src/features/auth src/features/llm-routes src/features/llm-providers`).
- The jaeger trace-export warnings in test output are unrelated (no OTel sidecar in test env).

## Open items / next steps
1. **Push `main`** if you want it on the remote (not done this session).
2. **Migration `29` `MODELS_STATE`** is a carried-over placeholder. Regenerate it with
   `aerich migrate` in an environment that has a live database before relying on aerich's
   history diffing. The `upgrade`/`downgrade` DDL itself is correct.
3. **15-Factor VI (object storage)** — upgrade path is in `CONTEXT.md`: env-selectable storage
   backend (disk default, S3 when set), MinIO in docker-compose, rewrite `agencies/logo.py`,
   mock S3 in tests. Do it when a multi-replica deploy needs it.
4. **Grow EDA as needs appear** — add events/consumers per real need (YAGNI); an external broker
   only when a second service must consume the stream.

## Warning: auto-commit git hook
An auto-commit git hook is active in this repo. During the session it committed on its own and
swept unrelated pre-existing dirty files (`.claude/*`, `docs/superpowers/*`) into commits, and it
flipped `context.md` ↔ `CONTEXT.md` (now settled at `CONTEXT.md`). Decide whether to keep it. It
is likely wired via `graft` hooks (see recent commits `bdde859`/`1888e2a` and `.claude/`).

## Key files to read first
- `CONTEXT.md` — dated changelog of every step this session.
- `docs/assessment-methodology-compliance-2026-08-17.md` — the original gap report.
- `docs/plan-clean-architecture-routers-2026-08-17.md` — the batch plan.
